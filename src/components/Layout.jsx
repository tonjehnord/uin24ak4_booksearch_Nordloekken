export default function Layout({children}) {
    return (
        <>
        <header>
            <h1>Booksearch</h1>
        </header>
        <main>
            {children}
        </main>
        </>
    )

}